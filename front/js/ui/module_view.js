'use strict';


define(function () {
    var ModuleView = function (options) {
        this.$el = options.$el;
        this.modules = options.modules;
    };

    ModuleView.prototype = {
    };

    return ModuleView;
});
